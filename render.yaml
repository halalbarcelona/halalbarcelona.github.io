services:
  - type: web
    name: premium-cuts-ai-booking
    runtime: node
    rootDir: premium-cuts-ai-booking
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: SQUARE_ACCESS_TOKEN
        sync: false
      - key: SQUARE_ENVIRONMENT
        value: sandbox
